---
quick_id: 260914-o2p
description: Remove duplicated Add to Home screen action in nsite AppSheet
date: 2026-09-14
mode: quick
---

# Remove duplicated "Add to Home screen" action in nsite AppSheet

Commit 3c9d0cd (napplet share landing) added an unconditional
`SheetAction(Icons.Filled.Add, "Add to Home screen")` to the nsite `AppSheet`
in `android/app/src/main/java/app/myco/ui/screens/AppsScreen.kt`, directly
above the pre-existing one guarded by `site.state == "ready"`. A ready nsite
now shows the button twice on long-press.

## Tasks

1. Delete the unconditional `SheetAction(... "Add to Home screen" ...)` line in
   `AppSheet`; keep the `if (site.state == "ready")` block.
   - verify: `grep -c '"Add to Home screen"' AppsScreen.kt` returns 2
     (one in `AppSheet`, one in `NappletSheet`).
   - done: nsite sheet renders one "Add to Home screen" row.
