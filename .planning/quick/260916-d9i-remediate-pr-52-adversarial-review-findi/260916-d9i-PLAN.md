---
quick_id: 260916-d9i
description: Remediate PR #52 adversarial review findings H1-H2, M1-M12, L1-L15
date: 2026-09-16
mode: quick
branch: feat/napplet-runtime
spec: ./260916-d9i-REVIEW.md
files_modified:
  - myco-core/src/napplet.rs
  - myco-core/src/outbox.rs
  - myco-core/src/peer_relay.rs
  - myco-core/src/ip_source.rs
  - myco-core/src/content.rs
  - myco-core/src/runtime.rs
  - myco-core/src/user_key.rs
  - myco-napplet-runtime/src/seams.rs
  - myco-napplet-runtime/src/session.rs
  - myco-napplet-runtime/src/nap/mod.rs
  - myco-napplet-runtime/src/nap/outbox.rs
  - myco-napplet-runtime/src/nap/relay.rs
  - myco-napplet-runtime/src/nap/identity.rs
  - myco-napplet-runtime/src/nap/resource.rs
  - myco-relay/src/lib.rs
  - myco-blossom/src/lib.rs
  - nsite-deck/src/seams.rs
  - android/app/src/main/java/app/myco/NappletActivity.kt
  - android/app/src/main/java/app/myco/NsiteActivity.kt
  - android/app/src/main/java/app/myco/MainActivity.kt
  - docs/design/napplet/napplet-runtime.md
  - CHANGELOG.md
---

# PR #52 review remediation

The review (`260916-d9i-REVIEW.md`) is the spec: 29 findings, each with file:line,
scenario and fix. Six commits, in the review's fix order. Nothing outside the 29;
no reformatting.

## Rules for every task

- One atomic commit per task. Subjects in the branch's style (`fix(napplet): …`,
  `fix(outbox): …`, `fix(android): …`, `fix(relay): …`). No attribution trailers.
- Gate before each commit: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`,
  `cargo test`. Tasks touching `android/` also run `cd android && ./gradlew assembleDebug`
  and then `git status --short Cargo.lock` must print nothing (a `patch.crates-io` build
  perturbs the lockfile; `git checkout -- Cargo.lock` if it did).
- Never `adb install`. Never commit to `main`.
- Facts fixed by reading the tree (do not re-derive): `nostr::Url` **is** `url::Url`
  (re-exported; `url` 2.5 is already in `Cargo.lock`) — no new dependency for URL parsing.
  rustc is 1.90, so `Ipv6Addr::to_ipv4_mapped`, `is_unique_local`, `is_unicast_link_local`
  are stable; `Ipv4Addr::is_shared` is not — hand-roll 100.64/10. `myco-relay` has tokio with
  the workspace `sync` feature, so `tokio::sync::OnceCell` is available. Host tests in
  `myco-core/src/ip_source.rs` and `myco-core/src/outbox.rs` dial mock relays on
  `127.0.0.1` as Internet lanes, so the resolved-address guard must be opt-out for tests.

<tasks>

<task type="auto">
  <name>Task 1: H1 + H2 — no byte-indexed scheme strip; mesh relay URLs are rebuilt from the npub, never dialled as given</name>
  <files>myco-core/src/napplet.rs, myco-napplet-runtime/src/seams.rs, myco-napplet-runtime/src/nap/outbox.rs, myco-core/src/outbox.rs, myco-core/src/peer_relay.rs</files>
  <action>
**H1 — `NappletAddr::strip_scheme` (`myco-core/src/napplet.rs:115`).** Replace the
`rest.len() >= scheme.len() && rest[..scheme.len()].eq_ignore_ascii_case(scheme)` slice with
`rest.get(..scheme.len()).is_some_and(|head| head.eq_ignore_ascii_case(scheme))` and then
`rest = &rest[scheme.len()..]` only inside that branch (the prefix is now known to be ASCII,
so the boundary is safe). Rule stated in the doc comment: never index a `&str` by a length
derived from another string. Tests in the existing `mod tests`:
`a_non_ascii_pointer_is_refused_not_a_panic` — `NappletAddr::parse("nostré")`,
`parse("naddr1€€")`, `parse("napplet:€")`, `parse("nostr:€x")` all return `Err` (wrap in
nothing; a panic fails the test).

**H2 — one strict mesh-URL parser, one canonical builder, and the pool never sees an
external URL.**

1. `myco-napplet-runtime/src/seams.rs`: add `pub fn mesh_relay_npub(url: &str) -> Option<String>`
   (strict) and `pub fn mesh_relay_url(npub: &str) -> String` returning exactly
   `format!("ws://{npub}.fips:4870")` — byte-identical to `myco-core/src/ip_source.rs::mesh_relay_url`.
   Strict means: `nostr::Url::parse` succeeds; scheme is `ws`; `username()` is empty and
   `password()` is `None`; host is `Host::Domain` ending in `.fips` whose label starts with
   `npub1` and contains only `[a-z0-9]`; `port()` is `None` or `Some(4870)`; `path()` is `""`
   or `"/"`; no query, no fragment. Anything else is `None`. Keep `is_mesh_relay_url` lenient
   (host ends with `.fips` → `RelayLane::Mesh`), so a malformed `.fips` URL is classified
   Mesh and then refused by the strict parser — it must never fall through to Internet.
2. `myco-napplet-runtime/src/nap/outbox.rs::validate_relay_url`: for a `.fips` host, return
   `Ok(RelayLane::Mesh { url: mesh_relay_url(&npub) })` from the strict parser, else
   `Err("mesh relay URL must be ws://<npub>.fips:4870 with no userinfo or path")`. (The
   full rewrite of this function onto `nostr::Url` is Task 2; here only the `.fips` branch
   changes, so Task 2 rebases cleanly.)
3. `myco-core/src/outbox.rs::mesh_relay_npub` delegates to
   `myco_napplet_runtime::seams::mesh_relay_npub`. Every `RelayLane::Mesh { url }` arm in
   `query_lane`, `publish_lane` and the subscribe/pull path that hands `url` to
   `PeerRelayPool` (`request`, `send`) passes `&crate::ip_source::mesh_relay_url(&npub)`
   instead of the lane's string.
4. `myco-core/src/outbox.rs::relay_list_lanes`: run each `r` URL through
   `myco_napplet_runtime::nap::outbox::validate_relay_url` and drop `Err` — a hostile kind
   10002 can no longer mint a lane `RelayLane::from_url` would accept.
5. `myco-core/src/peer_relay.rs::spawn_or_get`: if `url != crate::ip_source::mesh_relay_url(npub)`,
   `tracing::warn!` and spawn `run` with the canonical URL. Belt to the braces above.

Tests (host): in `nap/outbox.rs` `private_relay_urls_are_refused` add the refusals
`ws://npub1peer.fips:4870@evil.example/`, `ws://npub1peer.fips:24243`,
`ws://npub1peer.fips:4870/path`, `ws://npub1peer.fips?x=1`, and the acceptances
`ws://npub1peer.fips` → Mesh `ws://npub1peer.fips:4870`, `ws://npub1peer.fips:4870/` → same.
In `myco-core/src/outbox.rs` tests: `a_hostile_relay_list_cannot_name_a_userinfo_mesh_url`
(kind 10002 with `["r","ws://npub1peer.fips:4870@evil.example/"]` and `["r","ws://npub1peer.fips:24243"]`
yields no lanes; `["r","ws://npub1peer.fips"]` yields `Mesh { url: mesh_relay_url("npub1peer") }`)
and `mesh_url_round_trips_between_crates`
(`seams::mesh_relay_npub(&ip_source::mesh_relay_url("npub1x")) == Some("npub1x")` and
`seams::mesh_relay_url("npub1x") == ip_source::mesh_relay_url("npub1x")`), so the two crates
cannot drift.

Commit: `fix(napplet): strip schemes without byte indexing; rebuild mesh relay URLs from the npub, never dial them as given`
  </action>
  <verify>
    <automated>cargo test -p myco-core napplet::tests::a_non_ascii_pointer_is_refused_not_a_panic && cargo test -p myco-napplet-runtime private_relay_urls_are_refused && cargo test -p myco-core outbox::tests::a_hostile_relay_list_cannot_name_a_userinfo_mesh_url && cargo test -p myco-core outbox::tests::mesh_url_round_trips_between_crates && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <done>`NappletAddr::parse` never panics on non-ASCII input. A `.fips` URL with userinfo, a port other than 4870, a path or a query is refused at `validate_relay_url` and dropped by `relay_list_lanes`; every mesh dial in `OutboxService` and `spawn_or_get` uses `ip_source::mesh_relay_url(&npub)`. All gates green.</done>
</task>

<task type="auto">
  <name>Task 2: M4 + M5 + M7 + L15 + M9 + M10 + L10 — kind-aware Library ops; private-host bypasses closed; relay, subscription and frame-queue caps</name>
  <files>myco-core/src/content.rs, myco-napplet-runtime/src/nap/outbox.rs, myco-core/src/outbox.rs, myco-napplet-runtime/src/session.rs, myco-napplet-runtime/src/nap/mod.rs, myco-napplet-runtime/src/nap/relay.rs, android/app/src/main/java/app/myco/NappletActivity.kt</files>
  <action>
**M4 — `Content::remove_from_library` (`content.rs:1338`).** It is the nsite remover
(`forget_napplet` at `:1326` is the napplet one): add `i.kind == LibraryKind::Nsite` to the
`retain` predicate. **M5 — `Content::is_in_library` (`:3664`)**: add
`i.kind == LibraryKind::Nsite` to the `any` predicate. Tests beside
`an_nsite_and_a_napplet_with_one_d_tag_are_two_entries`:
`forgetting_the_nsite_keeps_its_napplet_twin` (add both `(X, "bitchat")` entries,
`forget_site` the nsite, the napplet entry with its `granted`/`pointer` survives) and
`a_napplet_entry_does_not_make_the_nsite_twin_installed` (napplet-only Library,
`is_in_library(&SiteAddr{X,"bitchat"})` is false).

**M7 + L15 — `validate_relay_url` and `is_private_host` (`nap/outbox.rs:409–452`).**
Rewrite on `nostr::Url`: parse (`Err` → "relay URL is not a URL"); scheme must be `ws`/`wss`;
non-empty `username()` or `Some` `password()` → `Err("relay URL must not carry userinfo")`;
`host()` `None` → `Err`. `Host::Domain(d)`: the `.fips` branch from Task 1; else refuse
`localhost`, `*.localhost`, `*.local`; else `Internet { url: url.to_string() }` (the string
as given — existing tests compare it). `Host::Ipv4`/`Host::Ipv6` → refuse when the new
`pub fn is_private_ip(ip: std::net::IpAddr) -> bool` says so. That predicate: v4 —
`is_loopback`, `is_private`, `is_link_local`, `is_unspecified`, `is_broadcast`,
`is_multicast`, first octet 0, CGNAT (`o[0] == 100 && (o[1] & 0xc0) == 64`); v6 —
`is_loopback`, `is_unspecified`, `is_unique_local`, `is_unicast_link_local`, `is_multicast`,
and `to_ipv4_mapped()` → recurse on the v4 (do **not** use `to_ipv4()`, which maps `::1` to
`0.0.0.1`). The url crate already normalises `127.1`, `2130706433`, `0x7f000001` and
`0177.0.0.1` to `Ipv4(127.0.0.1)`, so no shorthand handling is needed — assert it in tests.
Extend `private_relay_urls_are_refused` with: `ws://x@127.0.0.1:4870`,
`ws://[::ffff:192.168.1.2]:4870`, `ws://127.1`, `ws://2130706433`, `ws://0x7f000001`,
`ws://100.64.0.1`, `ws://224.0.0.1`, `ws://[ff02::1]`, `ws://0.0.0.0`, `ws://user:pw@relay.example`.

Resolved-address check (DNS rebinding, the review's `to_socket_addrs` step) lives at the
dial site, because the runtime crate has no tokio dependency and the resolve is async:
`myco-core/src/outbox.rs` — add `async fn dials_public(url: &str) -> bool` that parses with
`nostr::Url`, refuses userinfo, then `tokio::net::lookup_host((host, port_or_known_default))`
and returns false if the lookup fails or **any** resolved `IpAddr` is
`myco_napplet_runtime::nap::outbox::is_private_ip`. `OutboxService` gains
`guard_private_dials: bool` (true in `new`; `#[cfg(test)] fn allowing_private_dials(self) -> Self`
sets false — apply it in the existing tests that dial `mock_relay()` on `127.0.0.1`). Call the
guard in every `RelayLane::Internet` arm before `query_relay_filters` / `publish_to_relay`
(query, publish, pull). Log at debug with the URL. Note in the doc comment that
`connect_async` resolves again (TOCTOU accepted for now; a pre-connected stream via
`client_async_tls_with_config` is the follow-up). Test:
`an_internet_lane_that_resolves_private_is_not_dialled` — guard on, lane
`ws://localhost:1` and `ws://127.0.0.1:1` → `query_lane` yields `None`, `publish_lane` false,
without a connect attempt (assert quickly: well under the connect timeout).

**M9 — `hint_lanes` (`nap/outbox.rs:387`).** `pub const MAX_HINT_RELAYS: usize = 10`; more
than that → `Err("at most 10 relays may be named")` before any validation. Test
`too_many_named_relays_are_refused` (11 distinct `wss://` URLs → error; 10 → ok).

**M10 — per-session subscription cap and no orphan on a failed backlog.**
`session.rs`: `pub const MAX_SUBSCRIPTIONS: usize = 64`; `subscribe_in` returns
`Result<(), String>` — `Err("too many live subscriptions (64); close one first")` when the
map is at the cap **and** the `(domain, sub_id)` key is not already present (replacement
stays allowed); `subscribe` follows. `nap/mod.rs::open_subscription`: keep register-before-read
(the doc comment's gap argument stands) but on a backlog `Err` call
`session.unsubscribe_in(domain, sub_id)` before returning the reason; propagate the cap
error as the `.closed` reason. `nap/relay.rs` named-relay path (`session.subscribe(...)` at
the `Some(_)` arm) handles the cap `Err` → `relay.closed { subId, reason }`. Tests in
`nap/mod.rs` or `session.rs`: `subscriptions_are_capped_per_session` (65 distinct ids: the
65th gets `<domain>.closed`, `subscription_count() == 64`, re-registering an existing id is
fine) and `a_failed_backlog_leaves_no_subscription` (a `RelayBackend` test double whose
`query` returns `Err`; after subscribe the count is 0 and the reply is `.closed`).

**L10 — `NappletActivity.inbound` (`NappletActivity.kt:94`).** Bounded: `Channel<String>(INBOUND_CAPACITY)`
with `private const val INBOUND_CAPACITY = 64` in the companion; in the listener, when
`inbound.trySend(frame).isFailure`, `Log.w(TAG, "napplet frame queue full; frame dropped")`
and drop it (the shim's own timeout answers the call). Doc-comment: the semaphore bounds FFI
concurrency; this bounds queue memory.

Commit: `fix(napplet): kind-aware Library ops; close the private-host bypasses; cap named relays, live subscriptions and the frame queue`
  </action>
  <verify>
    <automated>cargo test -p myco-core content::tests::forgetting_the_nsite_keeps_its_napplet_twin && cargo test -p myco-core content::tests::a_napplet_entry_does_not_make_the_nsite_twin_installed && cargo test -p myco-napplet-runtime private_relay_urls_are_refused && cargo test -p myco-napplet-runtime too_many_named_relays_are_refused && cargo test -p myco-napplet-runtime subscriptions_are_capped_per_session && cargo test -p myco-napplet-runtime a_failed_backlog_leaves_no_subscription && cargo test -p myco-core outbox::tests::an_internet_lane_that_resolves_private_is_not_dialled && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && (cd android && ./gradlew assembleDebug) && test -z "$(git status --short Cargo.lock)" && test "$(grep -c 'UNLIMITED' android/app/src/main/java/app/myco/NappletActivity.kt)" = 0</automated>
  </verify>
  <done>Forgetting an nsite leaves its napplet twin; a napplet-only entry does not install the nsite twin. Every bypass URL in M7 is refused; CGNAT and multicast are private; an Internet lane resolving to a private address is not dialled (tests opt out). More than 10 named relays, more than 64 live subscriptions, and a full frame queue are refused, and a failed backlog leaves no filter registered. Gates and `assembleDebug` green, `Cargo.lock` clean.</done>
</task>

<task type="auto">
  <name>Task 3: M12 + M8 + L9 + M6 + L11 — a renderer crash closes its window, not the app; session leak, subframe shell, review race, home-offer</name>
  <files>android/app/src/main/java/app/myco/NappletActivity.kt, android/app/src/main/java/app/myco/NsiteActivity.kt, android/app/src/main/java/app/myco/MainActivity.kt, myco-core/src/runtime.rs</files>
  <action>
**M12 — `onRenderProcessGone` in both clients.** In `NappletWebViewClient`
(`NappletActivity.kt:354`) and `NsiteWebViewClient` (`NsiteActivity.kt:231`) override
`onRenderProcessGone(view: WebView, detail: RenderProcessGoneDetail): Boolean`: log
`detail.didCrash()` and `rendererPriorityAtExit()`, remove the WebView from its parent so
nothing touches the dead renderer, `(view.context as? Activity)?.finish()` (or an
`onRendererGone: () -> Unit` constructor callback — pick one, use it in both), and return
`true`. Returning true is what stops WebView from killing the process (API 26+ default). The
Activity's `onDestroy` already calls `webView.destroy()`; make sure it tolerates the view
having been detached.

**M8 — session leak during `nappletOpen` (`NappletActivity.kt:140`).** Make the open
non-cancellable and reconcile afterwards:
`val opened = withContext(Dispatchers.IO + NonCancellable) { client.nappletOpen(pointer) }`;
immediately after (before any suspension) `if (opened.ok && !isActive) { withContext(NonCancellable + Dispatchers.IO) { client.nappletClose(opened.sessionId) }; return@launch }`;
then assign `sessionId = opened.sessionId` **before** `mountShell()`. `onDestroy` keeps its
`sessionId.isNotEmpty()` close. Comment why: Back during the splash used to leave the
session (assembled HTML, outbox channel) in `NappletHost.sessions` forever.

**L9 — subframe navigation to the shell URL (`NappletActivity.kt:402`).** In
`shouldInterceptRequest`, serve the shell page only when `request.isForMainFrame`; a subframe
request for `/` or `/index.html` on the shell host gets the same 403 `WebResourceResponse`
as a foreign host, with a `Log.w` naming the path. Update the class doc: shell bytes land in
the main frame only, enforced here, not only by `shouldOverrideUrlLoading` (which Chromium
does not call for subframe http navigations).

**M6 — in-flight fetch overwrites `napplet_review` (`runtime.rs`, the spawned task in
`fetch_napplet`, `*review.lock().unwrap() = Some(outcome)`).** Write only if the slot still
belongs to this fetch: `let mut slot = review.lock().unwrap(); if slot.as_ref().is_some_and(|r| r.pointer == pointer) { *slot = Some(outcome); }`
else `tracing::info!` that the result was dropped because the sheet was dismissed or moved
on. Still call `content.refresh_napplet_status()` (the ingest happened). Test in
`runtime.rs` tests if a runtime fixture exists there; otherwise a focused unit test of the
predicate is acceptable — name it `a_stale_fetch_does_not_reopen_the_review`.

**L11 — `offerHomeScreenWhenNappletInstalled` (`MainActivity.kt:908`).** As the first step
inside the launched coroutine, read `core.state().library` once; if the napplet is already
there (`kind == Napplet && nappletPointer == pointer`) return **without** writing
`PREF_HOME_OFFERED` — a share of an installed napplet is not an install. Only a transition
from absent to present during the poll counts.

Commit: `fix(android): a renderer crash closes its window, not the app; no session leak on Back, no shell in a subframe, no stale review, no home offer for an installed napplet`
  </action>
  <verify>
    <automated>test "$(grep -c 'override fun onRenderProcessGone' android/app/src/main/java/app/myco/NappletActivity.kt android/app/src/main/java/app/myco/NsiteActivity.kt | awk -F: '{s+=$2} END {print s}')" = 2 && grep -q 'NonCancellable' android/app/src/main/java/app/myco/NappletActivity.kt && cargo test -p myco-core a_stale_fetch_does_not_reopen_the_review && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && (cd android && ./gradlew assembleDebug) && test -z "$(git status --short Cargo.lock)"</automated>
  </verify>
  <done>Both WebViewClients override `onRenderProcessGone`, finish their window and return true. Back during the splash closes the session it opened. A subframe request for the shell URL gets 403. A fetch result lands only in a review that still names its pointer. An already-installed napplet gets no home-screen dialog and no "asked" mark. Gates and `assembleDebug` green, `Cargo.lock` clean.</done>
</task>

<task type="auto">
  <name>Task 4: M1 + M2 + L5 + L3 + L2 + L12 + L13 — grants never widen past what was reviewed; safe key and relay-file writes; offline-only and own-events fixes</name>
  <files>myco-core/src/content.rs, myco-core/src/napplet.rs, myco-core/src/runtime.rs, myco-core/src/user_key.rs, myco-relay/src/lib.rs</files>
  <action>
**M1 — a napplet update cannot widen its own grants.** The Library records what was
reviewed, and launch widening is bounded by it.
- `content.rs`: `LibraryItem` gains `#[serde(default)] pub reviewed: Vec<String>` — the
  declared `requires` the review sheet showed at install (Kotlin ignores the new key).
  `NappletGrants` gains `reviewed: Vec<String>`. `add_napplet_to_library` takes
  `requires: Vec<String>` and stores it as `reviewed` (a fresh review is a fresh decision, as
  `denied` already is). `napplet_grants` returns it; `set_napplet_grants` leaves it alone.
  `NappletHost::open` (the test wrapper at `napplet.rs:277`) fills `reviewed` from a new
  parameter or from the fixture's declared list — keep
  `an_installed_napplets_grants_widen_to_what_it_declared` green by passing
  `reviewed: ["shell","relay"]`.
- `runtime.rs::install_napplet`: source `requires` from `napplet_review` under the same lock
  read that already takes `title` (filter `r.pointer == pointer`).
- `napplet.rs::open_with` (`:337`): widen only over `effective_grants(&grants.reviewed)` —
  domains in neither `granted` nor `denied` that this build implements and the user saw
  (defaults included, as `effective_grants` does). Domains the pinned manifest declares that
  are **not** in `reviewed` stay ungranted and are returned on `OpenedNapplet` as
  `pub unreviewed: Vec<String>`. Rewrite the comment block: "what the user agreed to" is the
  reviewed list, not whatever the update check pinned.
- Route back through review: `NappletOpenRequest` (`runtime.rs:2395`) gains
  `review: Arc<Mutex<Option<NappletReview>>>` (set in `prepare_open_napplet`); in `run`,
  when `opened.unreviewed` is non-empty, set `*review = Some(NappletReview { pointer, loading: false, title, description: String::new(), requires: <pinned manifest's declared list>, grants: effective_grants(&that list), error: String::new(), holder: None })`
  and return `widened = true` so the JNI path calls `note_library_changed` and the sheet
  appears on the Apps screen; Install → `InstallNapplet` → `add_napplet_to_library` records
  the new `reviewed` + `granted`. Tests in `napplet.rs`:
  `an_update_that_declares_more_is_not_granted_until_reviewed` (fixture built with
  `NappletBuilder::new().requires(&["relay","mesh"])`; open with
  `reviewed: ["relay"]`, `granted: []` → `mesh` absent from `granted`, present in
  `unreviewed`; open again with `reviewed: ["relay","mesh"]` → granted).
  Existing entries with an empty `reviewed` (this branch only, unreleased) widen over
  defaults only; say so in a comment.

**M2 + L5 — `RelayStore::flush_legacy` (`myco-relay/src/lib.rs:117`).** Add
`legacy_flushed: tokio::sync::OnceCell<()>` and run the whole migration inside
`get_or_init` so a second concurrent entry point waits on the first (L5). Inside: take
`pending_legacy`; for each event `save_event`: `Ok(_)` (Success **or** Rejected — a
duplicate or a superseded replaceable has nothing to keep) counts as migrated, `Err` pushes
the event into `failed`. If `failed` is empty, rename to `events.json.migrated` as now;
otherwise write `failed` back to `events.json` atomically (temp + rename) and
`tracing::warn!(failed = failed.len(), ...)` — the next launch retries them (M2).
`count()` keeps adding `pending_legacy.len()`. Test `a_failed_legacy_save_keeps_the_file`:
add `#[cfg(test)] fn open_with_map_size(dir, map_size: usize)` (refactor `open` to call it
with `MAP_SIZE`); open with a 1 MiB map and an `events.json` of enough 1 KiB text notes to
overflow it (start at 2 000; tune until at least one save errs); after one `query`,
`events.json` still exists and `events.json.migrated` does not; reopen with `open` (full
map) and the remaining notes migrate and the file moves aside.

**L3 — `user_key.rs::write_private` (`:118`) and the silent load.** Write to
`path.with_extension("tmp")` opened with `OpenOptions::new().write(true).create(true).truncate(true)`
plus `.mode(0o600)` via `std::os::unix::fs::OpenOptionsExt` on unix (so the file is never
world-readable, not even briefly), `write_all` + `sync_all`, then `rename` onto `KEY_FILE`.
Test `the_key_is_written_atomically_and_private`: no `.tmp` left behind; on unix the mode is
0600; a `.tmp` planted before the call is overwritten, not read. In
`runtime.rs::napplet_context` (`:1630`) replace `.ok()?` with a `match` that
`tracing::error!(error = %e, "user key unreadable; napplets cannot open")` before `return None`.

**L2 — `fetch_napplet` honours offline-only (`runtime.rs:1451`).** Capture
`content.is_offline_only()` before the spawn (as the update check at `:939` does); push
`addr.public_source()` only when it is false. Offline-only with no holder → the outcome is
the error `"Offline-only is on and nobody nearby shared this app"` without a fetch.

**L12 — first-use profile and relay list go to the local store only (`runtime.rs:1644`).**
Publish through `content.relay_store()` (`Option<Arc<RelayStore>>`, which implements
`RelayBackend`); when `None` (custom relay configured) skip with a debug log — the comment
already says "local relay only"; make the code match.

**L13 — `wipe_cache` keeps the user's own kind 0 and 10002 (`content.rs:3718`).**
Signature `wipe_cache(&self, keep_author: Option<PublicKey>)`; when `Some`, query the relay
for `Filter::new().author(pk).kinds([Kind::Metadata, Kind::RelayList])` and add those ids to
`keep_events`. `runtime.rs::wipe_cache` passes the user pubkey only if
`user_key::exists(data_dir)` (never generate a key on a wipe). Update the two existing
`wipe_cache` tests to pass `None`; add `wipe_cache_keeps_the_users_profile_and_relay_list`
(a kind 0 and a kind 10002 by the kept author survive; the same kinds by another author go).

Commit: `fix(napplet): grants never widen past what was reviewed; relay migration keeps what failed; private atomic key write; offline-only, local-only first-use events, wipe keeps own profile`
  </action>
  <verify>
    <automated>cargo test -p myco-core napplet::tests::an_update_that_declares_more_is_not_granted_until_reviewed && cargo test -p myco-core napplet::tests::an_installed_napplets_grants_widen_to_what_it_declared && cargo test -p myco-relay a_failed_legacy_save_keeps_the_file && cargo test -p myco-relay a_legacy_events_json_is_migrated_once && cargo test -p myco-core user_key::tests::the_key_is_written_atomically_and_private && cargo test -p myco-core content::tests::wipe_cache_keeps_the_users_profile_and_relay_list && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <done>A pinned update declaring a domain the user never reviewed opens without it and raises the review sheet; installing records the new reviewed set. `events.json` survives any failed save and concurrent first calls serialise. `user.nsec` is written 0600 via temp+rename and a corrupt key is logged. Offline-only napplet fetches ask only the sharer. First-use kind 0/10002 go to the embedded store only and survive `wipe_cache`. All gates green.</done>
</task>

<task type="auto">
  <name>Task 5: M3 + L1 + L7 + L4 + L6 + L8 — the shim's relay field, refusals as closed, getRelays, upper-case naddr, whole-body SVG sniff, size before read</name>
  <files>myco-napplet-runtime/src/nap/relay.rs, myco-napplet-runtime/src/nap/identity.rs, myco-core/src/napplet.rs, myco-napplet-runtime/src/nap/resource.rs, nsite-deck/src/seams.rs, myco-blossom/src/lib.rs</files>
  <action>
**M3 — `relay.subscribe` reads the field the shim sends (`nap/relay.rs:150`).** The
vendored shim (`assets/vendor/napplet-shim-prelude.global.js`, `subscribe5`) sends
`relay` **top-level** and never `options`. Read `message.field("relay")` first and fall back
to `options.relay` (lenient to both spellings). Fix the doc comment. Update
`subscribe_pulls_the_pool_or_the_named_relay` (`:611`, `:634`) to send top-level `relay`
and add one assertion that the `options.relay` spelling still works.

**L1 — subscribe refusals go out as `relay.closed` (`:146`).** An unparseable filter
returns `Envelope::new("relay.closed").with_field("subId", sub_id).with_field("reason", e)`
— the frame the shim's `subscribe5` listens for — mirroring `outbox.subscribe` (`nap/outbox.rs:193`).
A missing `subId` (`:141`) keeps `to_error`: there is no id to close. Test
`a_bad_filter_closes_the_subscription` (reply type is `relay.closed`, carries `subId`, and
`subscription_count()` is unchanged).

**L7 — `identity.getRelays` answers the user's kind 10002 (`nap/identity.rs:28`).** New
`async fn get_relays`: `ctx.signer.public_key()` (`Err` → `{}`); query `ctx.relay` with
`Filter::new().author(pk).kind(Kind::RelayList).limit(1)`, take the newest by `created_at`;
map each `["r", url]` / `["r", url, marker]` tag to the NIP-07 shape
`{ url: { read, write } }` — no marker → both true, `read` → read only, `write` → write only.
Test `get_relays_reflects_the_users_relay_list` (publish a kind 10002 signed by the test
signer's keys with `["r","wss://a"]`, `["r","wss://b","read"]`; expect
`{"wss://a":{"read":true,"write":true},"wss://b":{"read":true,"write":false}}`; with none
published, `{}`).

**L4 — upper-case `NADDR1…` (`myco-core/src/napplet.rs:66`).** In `NappletAddr::parse`,
detect the prefix with `pointer.get(..6).is_some_and(|p| p.eq_ignore_ascii_case("naddr1"))`
(no byte indexing — H1's rule) and decode `pointer.to_ascii_lowercase()` (bech32 is valid
all-upper or all-lower; mixed case is not, so lowering is correct). Same for an `NPUB1…`
segment in the `<npub>:<dtag>` shorthand — lower the npub only, never the d tag. Test
`an_upper_case_naddr_from_an_alphanumeric_qr_parses` (`parse(&naddr.to_uppercase())`
equals `parse(&naddr)`; `"NOSTR:" + upper` too).

**L6 — SVG sniff across the whole body (`nap/resource.rs:345`).** When the first 1 024
bytes decode as UTF-8 (tolerate a split multibyte char via `Utf8Error::valid_up_to`), scan
the **entire** slice with `bytes.windows(4).any(|w| w.eq_ignore_ascii_case(b"<svg"))` — no
allocation, O(n) over at most `MAX_BYTES`. Update the doc comment ("raw SVG is refused"
is now true). Test `an_svg_past_the_first_kilobyte_is_still_svg` (`<?xml …?>` + a 1 100-byte
comment + `<svg><script>` → `sniff_mime` says `image/svg+xml` and `fetch` answers
`blocked-by-policy`).

**L8 — size before read (`nap/resource.rs:182` vs `:220`).** `nsite-deck/src/seams.rs`:
add to `BlobStore` a provided method
`async fn size(&self, sha256_hex: &str) -> anyhow::Result<Option<u64>>` whose default calls
`get` and returns the length (so `MemBlobs`, `SourceBlobs`, `RemoteBlobStore` need nothing);
`myco-blossom/src/lib.rs` overrides it with `std::fs::metadata(path).len()` (`NotFound` →
`Ok(None)`). `resource::fetch` calls `ctx.blobs.size(&sha)` first and returns `too-large`
before any `get`. Tests: in `myco-blossom`, `size_matches_what_was_put`; in `resource.rs`,
`an_oversized_local_blob_is_refused_without_being_read` (a `CountingBlobs` wrapper around
`MemBlobs` counting `get` calls, `size` delegating; an over-`MAX_BYTES` blob → `too-large`
with zero `get` calls).

Commit: `fix(napplet): read the shim's relay field; refuse subscribes as closed; answer getRelays; upper-case naddr; SVG sniff over the whole body; size before read`
  </action>
  <verify>
    <automated>cargo test -p myco-napplet-runtime subscribe_pulls_the_pool_or_the_named_relay && cargo test -p myco-napplet-runtime a_bad_filter_closes_the_subscription && cargo test -p myco-napplet-runtime get_relays_reflects_the_users_relay_list && cargo test -p myco-core napplet::tests::an_upper_case_naddr_from_an_alphanumeric_qr_parses && cargo test -p myco-napplet-runtime an_svg_past_the_first_kilobyte_is_still_svg && cargo test -p myco-napplet-runtime an_oversized_local_blob_is_refused_without_being_read && cargo test -p myco-blossom size_matches_what_was_put && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <done>A named relay sent the way the shim sends it targets that relay. A bad filter closes the subscription on the frame the shim listens for. `getRelays` returns the user's NIP-65 map. `NADDR1…`/`NOSTR:NADDR1…` parse. SVG anywhere in a text body is refused. An oversized local blob is refused from its size, never read. All gates green.</done>
</task>

<task type="auto">
  <name>Task 6: M11 interim + L14 + docs + CHANGELOG — refuse identity-shaping kinds under the relay grant; document secure context and accepted policy</name>
  <files>myco-napplet-runtime/src/nap/relay.rs, docs/design/napplet/napplet-runtime.md, CHANGELOG.md</files>
  <action>
**M11 — interim per-call refusal in `sign_template` (`nap/relay.rs:236`).** `sign_template`
is the one parser behind `relay.publish`, `outbox.publish` and `mesh.publish`, so the
refusal lands there. Add `pub const REFUSED_KINDS: &[u16] = &[0, 3, 5]` and
`fn kind_needs_a_prompt(kind: u16) -> bool` = `REFUSED_KINDS.contains(&kind) || (10_000..=19_999).contains(&kind)`
(covers 10002 and every replaceable kind). Right after the kind is parsed, refuse with
`Err(format!("kind {kind} rewrites the user's profile, contacts, relay list or deletes their events; not allowed under the relay grant until per-event prompts exist"))`
— the caller's `failed(...)` turns it into `ok: false` on the `.result` frame the shim
surfaces as a rejected promise; never silent. Do not touch the runtime's own kind 0/10002
publishes (`runtime.rs::napplet_context`), which do not go through `sign_template`. Tests
`identity_shaping_kinds_are_refused_under_the_relay_grant` (kinds 0, 3, 5, 10002, 10050,
19999 → `ok: false` with the reason, nothing reaches the sink; kinds 1, 9, 20666, 30023 still
sign) and one `outbox.publish` case for kind 10002.

**Docs — `docs/design/napplet/napplet-runtime.md`.**
- D8 row: replace "A granted `relay` covers publishing — no per-event prompt." with the
  interim: kinds 0, 3, 5 and 10000–19999 are refused per call until the permission model
  adds prompts; and add "A launch may grant a declared domain … only if it was on the
  reviewed list; a later manifest declaring more goes back through the review sheet" (M1).
- §5.2 (shell) or §7.5: a new short subsection **Secure context** (L14): `*.localhost` is
  potentially trustworthy and the `srcdoc` frame inherits it, which unlocks
  `navigator.clipboard.writeText` on a tap and `crypto.subtle`; camera, microphone,
  geolocation and clipboard-read stay denied because no `WebChromeClient` grants them —
  keep it that way, and any secure-context API WebView adds later is reviewed against this
  note.
- §S2's relay-policy paragraph: append an **Accepted policy** list, verbatim in spirit from
  the review: napplet-named internet relays are an exfiltration channel by design;
  `relay.subscribe` is unscoped across napplets and the user's events; `relay.publish` signs
  any kind except the M11 interim set; all three are roadmap items under the unified
  permission model. Also note that `validate_relay_url` now parses with the `url` crate,
  refuses userinfo, unwraps v4-mapped v6, refuses CGNAT and multicast, and that the outbox
  refuses an internet lane whose name resolves to a private address; and that "raw SVG is
  refused" is checked over the whole body.
- §7.4: a `.fips` relay URL is only ever dialled as `ws://<npub>.fips:4870` rebuilt from
  its npub (H2).

**CHANGELOG — `[Unreleased]`.** Napplets are unreleased, so the napplet-side fixes fold
into the existing **Napplets** entry, not "Fixed": in the *Permissions* paragraph, say that a
napplet update asking for more goes back through the review sheet before it gets it, and
that posting as you excludes your profile, contacts, relay list and deletions for now. Under
`### Fixed` add one line for released behaviour: an nsite window whose page renderer
crashed took the whole app with it; the window now closes on its own.

Commit: `fix(napplet): refuse profile, contacts, deletion and replaceable kinds under the relay grant; document secure context and accepted policy`
  </action>
  <verify>
    <automated>cargo test -p myco-napplet-runtime identity_shaping_kinds_are_refused_under_the_relay_grant && grep -q 'Secure context' docs/design/napplet/napplet-runtime.md && grep -q 'Accepted policy' docs/design/napplet/napplet-runtime.md && grep -q 'renderer' CHANGELOG.md && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <done>Kinds 0, 3, 5 and 10000–19999 fail `relay.publish`/`outbox.publish`/`mesh.publish` with a surfaced reason; other kinds sign. The design doc carries the M11 interim in D8, a Secure context note, the accepted-policy list and the H2/M7 validation rules; CHANGELOG has the permissions wording and the renderer-crash fix. All gates green.</done>
</task>

</tasks>

## Coverage

| Finding | Task | Finding | Task | Finding | Task |
|---|---|---|---|---|---|
| H1 | 1 | M6 | 3 | L5 | 4 |
| H2 | 1 | M7 | 2 | L6 | 5 |
| M1 | 4 | M8 | 3 | L7 | 5 |
| M2 | 4 | M9 | 2 | L8 | 5 |
| M3 | 5 | M10 | 2 | L9 | 3 |
| M4 | 2 | M11 | 6 | L10 | 2 |
| M5 | 2 | M12 | 3 | L11 | 3 |
| L1 | 5 | L2 | 4 | L12 | 4 |
| L3 | 4 | L4 | 5 | L13 | 4 |
| L14 | 6 | L15 | 2 | | |

## Verification

After Task 6: `just test` (390+ new tests green), `cargo ndk` check is covered by
`assembleDebug` in Tasks 2 and 3. `git log --oneline main..` shows six new `fix(...)`
commits, none with an attribution trailer. `Cargo.lock` clean.

Not host-testable, say so in the PR: M12 (renderer kill on a real device — force a
renderer OOM in a napplet and in an nsite; the window closes, the app and the mesh node
stay up), M8 (Back during the splash; `open_count()` returns to 0 in the dev screen), L10
(a napplet spamming `postMessage`; memory stays flat, frames are dropped with a log line),
M2 with a real pre-LMDB `events.json` before flashing.

## Threat model

| Threat | Category | Component | Severity | Disposition |
|---|---|---|---|---|
| T-d9i-01 remote crash via non-ASCII pointer | DoS | `NappletAddr::strip_scheme` | high | mitigate (Task 1) |
| T-d9i-02 Circle peer pool hijacked to an internet host | Spoofing/Tampering | `validate_relay_url`, `relay_list_lanes`, `PeerRelayPool` | high | mitigate (Task 1) |
| T-d9i-03 SSRF / ungated loopback relay via host bypasses and DNS rebinding | Elevation | `validate_relay_url`, `OutboxService` | high | mitigate (Task 2); connect-time re-resolve accepted as interim |
| T-d9i-04 renderer crash kills the process | DoS | both WebViewClients | high | mitigate (Task 3) |
| T-d9i-05 silent grant widening on update | Elevation | `open_with` | medium | mitigate (Task 4) |
| T-d9i-06 identity rewrite / deletion under default grant | Tampering | `sign_template` | medium | mitigate interim (Task 6); full prompts on the roadmap |
| T-d9i-07 resource exhaustion from inside the frame | DoS | `hint_lanes`, `Session`, `inbound` | medium | mitigate (Task 2) |
| T-d9i-SC | Tampering | cargo installs | low | accept — no new packages; `url` is already resolved in `Cargo.lock` |
